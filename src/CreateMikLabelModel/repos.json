{
  "repos": [
    // Each array of repo entries is considered a "set" that will be scanned together to generate one model.
    // If an array has more than one repo, the first item is the target repo containing the labels.
    // Typically, the repos after the first are archived or ancillary repos that contain useful material for generating models.
    [
      "Azure/azure-sdk-for-net"
    ]
  ]
}
